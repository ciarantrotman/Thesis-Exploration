using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ContextualShellLineRenderer : MonoBehaviour {
    /*
    private GameObject ContextualShell;
    private GameObject InputController;
    private bool _shellActive;


    void Start()
    {

        contextualLineRenderer = GetComponent<LineRenderer>();
    }

    void Update()
    {
        contextualLineRenderer.enabled = true;
        Vector3 StartPoint = ContextualShell.transform.position;
        Vector3 EndPoint = InputController.GetComponent<IndirectGrab>().SelectedObject.transform.position;
        Debug.Log(EndPoint);
        contextualLineRenderer.SetPosition(0, StartPoint);
        contextualLineRenderer.SetPosition(1, EndPoint);
    }*/
}
