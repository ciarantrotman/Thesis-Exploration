using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TEMP : MonoBehaviour {
    /* 
     if (Input.GetKeyDown(KeyCode.UpArrow))
            {
                Invoke("EgoToExo", 0);
            }
            if (Input.GetKeyDown(KeyCode.DownArrow))
            {
                Invoke("ExoToEgo", 0);
            }
    #region Content Locking Logic
    public void ExoToEgo()
    {
        Debug.Log(SelectedObject.transform.name);
        if (EgocentricOrigin != null && SelectedObject != null)
        {
            SelectedObject.transform.parent = EgocentricOrigin.transform;
        }
    }
    public void EgoToExo()
    {
        Debug.Log("NEET");
        if (EgocentricOrigin != null && SelectedObject != null)
        {
            SelectedObject.transform.parent = null;
        }
    }
    #endregion
}
*/
}
